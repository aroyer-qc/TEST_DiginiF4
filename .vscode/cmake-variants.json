{
  "buildType": {
    "default": "release",
    "description": "Build Type",
    "choices": {
      "debug": {
        "short": "Debug",
        "buildType": "Debug"
      },
      "release": {
        "short": "Release",
        "buildType": "Release"
      }
    }
  }
}